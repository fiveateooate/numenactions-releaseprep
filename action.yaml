name: 'NumenActions NumenPackage'
description: 'Run numen packager'
runs:
  using: 'docker'
  image: 'docker://ghcr.io/fiveateooate/numenactions:latest'
  entrypoint: '/usr/local/bin/numencli'
  args:
    - --loglevel
    - debug
    - releaseprep
